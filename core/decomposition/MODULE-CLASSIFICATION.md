# Module Classification

A module has one clear responsibility, explicit owned data, explicit dependencies, and defined public interfaces. Application shells and external infrastructure are classified separately from domain modules when they do not own business state.
