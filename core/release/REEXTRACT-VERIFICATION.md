# Re-extract Verification

A release is not accepted from ZIP creation alone. The archive is integrity-tested, extracted into a fresh directory, and compared with staging by exact relative regular-file set plus SHA-256 for every file. Any path-set or digest difference is a release-integrity failure.
