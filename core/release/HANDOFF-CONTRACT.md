# Handoff Contract

A build-ready handoff identifies canonical authorities, starts at `WP-000`, exposes deterministic commands and acceptance evidence, contains zero pending protected decisions, contains no stale required derived artifacts, and has zero unresolved Critical or High audit blockers. Runtime implementation validation and production readiness are separate later assurance levels.
