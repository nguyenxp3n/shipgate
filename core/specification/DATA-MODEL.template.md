# Data Model Template

For each authoritative data object record owner, identity, lifecycle, relationships, validation invariants, retention/deletion/anonymization/export behavior, transaction boundaries, derived copies, and recovery implications. Physical schema representation is owned by the project-assigned machine/implementation authority; this document owns data semantics where the Authority Matrix assigns it.
