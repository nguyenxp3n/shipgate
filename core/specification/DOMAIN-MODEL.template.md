# Domain Model Template

Define canonical terminology, actors, aggregates/entities/value objects, relationships, ownership, state machines, invariants, and business events. Every term used by public contracts should map to one canonical concept. Avoid physical storage detail unless it is itself a domain invariant.
