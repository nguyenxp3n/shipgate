# Generic Core

The Generic Core is project-class agnostic. It defines lifecycle, typed authority, intake and normalization rules, governance, module decomposition, interaction contracts, testing policy, work-package semantics, audit closure, build-readiness semantics, and release integrity.

Web-specific assumptions belong in `profiles/web-saas/`, not here.

Normative inventory: `core/CORE-MANIFEST.yaml`.
