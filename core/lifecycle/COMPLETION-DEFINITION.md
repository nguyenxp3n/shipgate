# Completion Definition

State names are assurance levels, not marketing labels. `BUILD_READY` means coding-agent handoff is complete. `FINAL_RELEASE` requires the release gate; neither implies target-runtime or production validation.
