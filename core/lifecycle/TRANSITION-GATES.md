# Transition Gates

A transition is allowed only when the target is explicitly legal and every recorded blocking gate is clear. Critical/high blockers and unresolved protected decisions prevent readiness transitions.
