# Phase-Aware Test Gates

A future test is not required before the responsible implementation phase exists. Each critical invariant declares `active_from_wp` or an equivalent activation condition. Once the responsible Work Package is READY/complete, its required test classes and acceptance commands become mandatory. This prevents both false early failures and late omission of critical tests.
