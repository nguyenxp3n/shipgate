# Precedence Rules

Resolve conflicts by the Authority Matrix for the affected subject. A document does not outrank a machine contract outside the subject it owns. Composed authority must declare merge semantics explicitly.
