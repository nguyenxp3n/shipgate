# Input Inventory

Inventory every regular source file with relative path, byte size, media class, and SHA-256. Symlinks and paths escaping the declared input root are rejected. Inventory order is deterministic.
