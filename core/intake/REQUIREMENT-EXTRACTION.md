# Requirement Extraction

Discovery records each explicit requirement with source path and location provenance. It preserves source meaning and does not silently reconcile missing or conflicting requirements.
