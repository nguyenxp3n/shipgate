# ShipGate — Deterministic Quality Gate & Project Delivery Engine

`ShipGate` turns heterogeneous project material into an auditable, coding-agent-ready specification package. It provides a deterministic core plus a production-grade `web-saas` profile.

The approved architecture lives in `docs/superpowers/specs/shipgate-architecture-spec.md`.
