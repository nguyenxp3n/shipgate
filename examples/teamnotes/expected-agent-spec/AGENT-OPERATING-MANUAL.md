# TeamNotes Agent Operating Manual

Read `AUTHORITY-MATRIX.yaml`, then the READY work-package graph. Implement only dependency-eligible packages. Public API, database/migrations, authentication, authorization, tenant isolation, retention, event contracts, deployment topology, and irreversible data changes are protected decisions. Historical audit material is advisory only. Use the canonical Taskfile command gateway and stop when a protected ambiguity is encountered.
