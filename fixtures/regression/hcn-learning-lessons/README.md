# Genericized regression lessons

These fixtures preserve structural failure classes discovered in a prior complex documentation project. They intentionally use neutral domains (`accounts`, `rewards`, `progress`, `worker`) and contain no project-specific product rules. Each fixture is executable evidence that Workflow V2 rejects the corresponding defect class.
