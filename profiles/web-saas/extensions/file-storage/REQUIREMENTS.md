# file-storage Extension Requirements

Define detected MIME/content validation, size/count bounds, storage visibility, authorization, signed URL lifetime, malware/active content posture, image metadata/processing, retention/deletion and orphan cleanup.

Enabled means the complete obligation pack applies; disabled means coding agents must not introduce this surface speculatively.
