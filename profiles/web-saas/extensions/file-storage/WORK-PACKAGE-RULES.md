# file-storage Work Package Rules

Any WP implementing this extension must reference `file-storage` authority, declare security/data/API impact, activate the extension test classes, and stop on newly discovered protected decisions.
