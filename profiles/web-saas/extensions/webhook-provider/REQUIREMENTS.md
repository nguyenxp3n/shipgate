# webhook-provider Extension Requirements

For inbound/outbound webhook integration define authentication/signature, raw payload requirements, replay protection, deduplication, ordering, unknown events, retry delivery, payload bounds and reconciliation.

Enabled means the complete obligation pack applies; disabled means coding agents must not introduce this surface speculatively.
