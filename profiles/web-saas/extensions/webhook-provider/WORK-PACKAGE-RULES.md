# webhook-provider Work Package Rules

Any WP implementing this extension must reference `webhook-provider` authority, declare security/data/API impact, activate the extension test classes, and stop on newly discovered protected decisions.
