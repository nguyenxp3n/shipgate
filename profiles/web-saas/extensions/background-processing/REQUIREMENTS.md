# background-processing Extension Requirements

Define queue/job payload schema, idempotency, retry/backoff/jitter, timeout/cancellation, dead-letter/poison handling, concurrency/ordering, worker deployment and observability.

Enabled means the complete obligation pack applies; disabled means coding agents must not introduce this surface speculatively.
