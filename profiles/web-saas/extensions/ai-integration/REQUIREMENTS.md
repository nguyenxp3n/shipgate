# ai-integration Extension Requirements

Define provider/model role, prompt/tool ownership, data sent externally, PII/retention policy, moderation if applicable, timeout/retry/fallback, cost/rate budgets, streaming, output validation and an explicit AI authority boundary. AI output is non-authoritative over protected business/security state by default.

Enabled means the complete obligation pack applies; disabled means coding agents must not introduce this surface speculatively.
