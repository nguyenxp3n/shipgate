# payments Extension Requirements

Provider payment/subscription state and application entitlement state are distinct. Define webhook signature verification, replay protection, event dedupe, out-of-order handling, idempotency, entitlement transition ownership, reconciliation and provider outage behavior.

Enabled means the complete obligation pack applies; disabled means coding agents must not introduce this surface speculatively.
