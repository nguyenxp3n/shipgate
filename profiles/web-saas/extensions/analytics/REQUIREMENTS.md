# analytics Extension Requirements

Treat analytics as derived unless explicitly approved otherwise. Define event provenance, PII minimization, consent/purpose, sampling, retention, external transfer, deletion propagation and provider failure behavior.

Enabled means the complete obligation pack applies; disabled means coding agents must not introduce this surface speculatively.
