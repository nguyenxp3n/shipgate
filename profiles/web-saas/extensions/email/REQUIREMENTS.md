# email Extension Requirements

Separate business transaction commit, notification intent and delivery attempt. Define template ownership, provider failure/retry, suppression/preferences, security-token email semantics, enumeration resistance and delivery observability.

Enabled means the complete obligation pack applies; disabled means coding agents must not introduce this surface speculatively.
