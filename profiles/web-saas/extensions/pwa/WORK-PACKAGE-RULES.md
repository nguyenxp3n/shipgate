# pwa Work Package Rules

Any WP implementing this extension must reference `pwa` authority, declare security/data/API impact, activate the extension test classes, and stop on newly discovered protected decisions.
