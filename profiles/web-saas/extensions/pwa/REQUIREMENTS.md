# pwa Extension Requirements

Define install/update strategy, service-worker lifecycle, offline support, public/static cache rules, private data cache prohibition or approved secure lifecycle, background sync and logout/session-expiry cache cleanup.

Enabled means the complete obligation pack applies; disabled means coding agents must not introduce this surface speculatively.
