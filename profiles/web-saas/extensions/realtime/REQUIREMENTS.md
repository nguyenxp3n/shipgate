# realtime Extension Requirements

Define connection authentication and re-authentication, channel/resource authorization, reconnect/replay, ordering, heartbeat, backpressure, fanout/horizontal scaling and privacy/logging.

Enabled means the complete obligation pack applies; disabled means coding agents must not introduce this surface speculatively.
