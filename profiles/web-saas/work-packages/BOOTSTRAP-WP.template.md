# Bootstrap WP Template

Purpose: establish repository/toolchain/command gateway. Required acceptance: setup, format check, lint, typecheck, unit/integration/contract test entry points and CI skeleton. Stop on protected stack/deployment/security ambiguity.
