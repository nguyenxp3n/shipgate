# Configuration Contract

Every environment variable/config key must declare type, requiredness, allowed/default values, environments, secret classification, and owner. Secrets have no unsafe production defaults. Code-referenced configuration and documented required configuration must be cross-checked.
