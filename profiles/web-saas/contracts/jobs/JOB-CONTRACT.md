# Job Contract

Record producer, queue, payload schema/version, idempotency key, concurrency/ordering, retry/backoff/jitter, maximum attempts, dead-letter or poison handling, timeout/cancellation, authoritative writes, and metrics/logs. Business commits must not depend on an unacknowledged delivery assumption.
