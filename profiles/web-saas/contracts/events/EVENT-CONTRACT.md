# Event Contract

Events use a machine-readable envelope and payload schema with stable event ID, type/version, occurred time, producer, correlation/causation metadata as required, and payload. Record producer-time semantics, ordering guarantees, retry, deduplication/idempotency, retention/replay, and consumer failure behavior.
