# Webhook Contract

For each inbound webhook define raw-body/signature requirements, replay window, provider event identifier, deduplication, out-of-order handling, unknown-event behavior, retry response, processing transaction, asynchronous handoff, reconciliation, and observability. Never trust payload identity without provider authentication.
