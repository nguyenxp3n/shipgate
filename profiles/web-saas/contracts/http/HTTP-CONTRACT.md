# HTTP Contract

The project must freeze API base/versioning, media types, identifier/timestamp formats, validation strictness, canonical error envelope, authentication, backend authorization metadata, rate-limit response semantics, operation-level idempotency, pagination, retry safety, and transaction/result semantics. OpenAPI owns HTTP wire shape; normative prose owns business/security/transaction meaning.
