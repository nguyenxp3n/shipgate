# Session Contract

Define identity source, credential transport/storage, access/session lifetime, refresh/renewal, rotation, reuse detection, revocation, logout/logout-all, cross-tab coordination when relevant, account verification/recovery, brute-force/rate controls, CSRF and CORS applicability, and audit evidence. Never store reusable secrets in browser locations not explicitly approved by the security specification.
