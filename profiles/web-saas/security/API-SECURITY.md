# API Security

Require input validation, authentication, backend authorization, resource/tenant scope enforcement, canonical error handling without sensitive leakage, operation-specific rate limits, idempotency for retryable side effects, bounded payloads, dependency timeouts, and security logging/redaction. UI gating is not authorization.
