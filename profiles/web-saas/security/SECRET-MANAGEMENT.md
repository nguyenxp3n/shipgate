# Secret Management

Separate configuration from secrets/credentials/key material. Define storage, runtime injection, local development, CI handling, rotation, revocation, audit and redaction. Production templates must not ship live or unsafe default secrets.
