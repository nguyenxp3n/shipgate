# File Upload Security

When file upload is enabled require detected-content validation, bounded size/count, authorization, private-by-default storage unless product-approved, malware/active-content disposition, image metadata/processing rules, safe object names, download headers, signed URL expiry, retention and orphan cleanup.
