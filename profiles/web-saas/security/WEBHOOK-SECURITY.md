# Webhook Security

Require provider authentication/signature validation over the exact required payload representation, replay protection, timestamp/window rules, deduplication, out-of-order tolerance, unknown-event handling, rate/size bounds and auditability. Secrets must be rotatable without silently disabling verification.
