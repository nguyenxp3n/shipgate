# SSRF Policy

If user-controlled outbound URLs exist, resolve allowed schemes/hosts, redirect revalidation, DNS rebinding, loopback/private/link-local/cloud-metadata blocking, timeout, response-size limits, protocol handling and observability. If absent, record `NOT_APPLICABLE` with evidence rather than omitting review.
