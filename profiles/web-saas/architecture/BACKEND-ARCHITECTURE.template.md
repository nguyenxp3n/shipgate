# Backend Architecture Template

Freeze service/module topology, request orchestration, synchronous query/command boundaries, background jobs/events, transaction ownership, error mapping, configuration loading, observability, and external provider boundaries. Identify which module owns each durable business entity and which cross-module writes are forbidden.
