# Integration Architecture Template

For every cross-boundary interaction classify it as synchronous query, synchronous command, same-request orchestration, domain event, background job, outbox event, external webhook, or file/object transfer. Record owner, producer, consumer, authorization, timeout, retry, idempotency, ordering, failure, and reconciliation semantics.
