# Deployment Template

Specify immutable build/runtime artifact, environment/config injection, migration execution/order, worker/background compatibility, health/readiness gate, rolling/replace strategy, static assets/CDN, TLS termination, rollback/forward-fix path, and smoke checks. Topology is project-resolved.
