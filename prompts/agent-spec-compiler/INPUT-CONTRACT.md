# Input Contract

Consume only files selected by the role registry and controller. Treat historical audit material as advisory evidence unless this role explicitly includes it. Preserve source/provenance references in outputs.
