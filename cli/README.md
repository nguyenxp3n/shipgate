# CLI

The public command is `workflow`, implemented by `project_finalizer.cli`.

The command tree includes initialization, inspection, discovery, normalization, decision resolution, specification, architecture, contracts, agent-spec, work-package, audit, corrective, readiness, validation, status, transition, release, resume, and orchestration entrypoints.

Stable exit codes are part of the public automation contract.
