# Validators

Human-facing inventory for validation behavior. Executable validator implementations live under `src/project_finalizer/validators/`.

Validation layers cover syntax, structure, references, semantics, authority, contracts, security/profile obligations, work-package DAGs, audit closure, readiness, self-hosting, and release integrity.
