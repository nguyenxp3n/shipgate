# {{PROJECT_NAME}} Generic Coding Agent Bootstrap

This file is an adapter bootstrap, not product authority. Version: {{VERSION}}.

Read in this exact order before implementation:

1. Agent Operating Manual — `{{OPERATING_MANUAL}}`
2. Authority Matrix — `{{AUTHORITY_MATRIX}}`
3. SPEC-MANIFEST — `{{SPEC_MANIFEST}}`
4. work-packages/README.md — `{{WORK_PACKAGES_README}}`
5. WP-000 — `{{WP000}}`

Use `{{COMMAND_GATEWAY}}` as the canonical command gateway. Follow work-package dependencies. Stop on protected ambiguity instead of inventing product/API/database/security/privacy/deployment decisions. Historical audit material is advisory only unless canonical authority explicitly incorporates a resolution.
