# Generators

Human-facing inventory for deterministic generators. Executable implementations live under `src/project_finalizer/generators/`.

Generators produce manifests, adapter bootstraps, indexes, ownership views, work-package graphs, audit summaries, readiness/QA reports, checksums, staging trees, and deterministic release archives. LLM-authored content must use explicit role contracts rather than hidden generator behavior.
